export class ForceDirectedParam{
    Focus: string; // none/column :: search first level column for specific value and recalculate scale from there
    Scale: string; // none/numericColumn
    Levels: string[];
}